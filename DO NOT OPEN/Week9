WEEK 8-> SImply used google gen ai 

Week 9-> 
Challenges
1.Report were getting prefetched, meaning the moment they get created, causing wrong report to be shown
Needed to fix polling logic

2.needed to fix the loading screen issue, had to check the endpoints and points that recieve to match properly

3.Quality and security score where linear based,converted them to non linear using log

4.While serving a react file as a static file with express, needed to make sure that the react files do not match
express routes, route checks for express routes to match then react 

build command->cd apps/web/stackprobe_frontend && npm ci && npm run build && cd ../../api/server && mkdir -p public && cp -R ../../web/stackprobe_frontend/dist/* public/ && npm ci

start command->cd apps/api/server && npm run start

5.needed to create docker to create and run apps that are not nodejs, basically worker requires git, eslint, which are not js, so we need to install these things, that is where docker comes in