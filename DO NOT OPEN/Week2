State is used to prevent CSRF Attack
### Tradeoffs

- Polling vs SSE:
  Chose polling for simplicity and reliability behind proxies; SSE could reduce latency but adds infra complexity.

- BullMQ vs Kafka:
  BullMQ chosen for simplicity and tight Redis integration; Kafka would be overkill for this scale.

- PostgreSQL vs NoSQL:
  Strong consistency and relational queries (jobs ↔ reports ↔ findings) made SQL a better fit.

  ### Failure scenarios

- Redis down → API rejects new jobs; existing jobs resume when Redis recovers
- Worker crash mid-job → BullMQ marks job as stalled and retries
- Git clone failure → job marked FAILED with reason
- ESLint timeout → process killed, job continues with partial analysis
- DB write failure → job retried (idempotent)

### Scaling

- Horizontal scaling via multiple workers
- Bottleneck: CPU-bound ESLint + network-bound git clone
- Optimization opportunities:
  - shallow clones (already used)
  - caching repo clones
  - parallelizing OSV + ESLint steps

Bugs I Fixed,
Wifi error,  College wifi blocks many ports - Fixed Not able to connect to Railway Database


Just like Repo are global, i have made jobs as global too, so if some one asks for the same repo_id as a job previously created then just return the same job_id back, 
but what about report distribtuion, reports are create per job, so new job new report, 

Now read this properly
If users enter repo url
then two things can happend
Job is running,pending
or completed 
in running or pending=> we can return safely and give them the corresponding report,
in completetd, -> we return it back, but also inform the user it is an old report so you can want to re run the analyis or not option
We are going to add something to request and that would be force:true or force false 

also we return job_id and status 

Question is how to handle the Reports that will part of week 3 
